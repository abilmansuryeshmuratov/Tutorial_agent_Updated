{
    "name": "@elizaos/plugin-bnb-mcp",
    "version": "0.1.0",
    "description": "BNB Chain MCP integration plugin for automated blockchain insights and Twitter updates",
    "main": "dist/index.js",
    "type": "module",
    "types": "dist/index.d.ts",
    "dependencies": {
        "@elizaos/core": "workspace:*",
        "viem": "^2.21.0",
        "tsup": "8.3.5"
    },
    "scripts": {
        "build": "tsup --format esm --dts",
        "dev": "tsup --format esm --dts --watch",
        "clean": "rm -rf dist"
    }
}